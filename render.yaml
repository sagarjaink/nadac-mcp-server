services:
  - type: web
    name: nadac-mcp-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HTTP_MODE
        value: true
    healthCheckPath: /health
